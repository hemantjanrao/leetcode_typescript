let sum = (x: number, y: number): number => {
    return x + y;
}

console.log(sum(10, 20)); //returns 30


let hello =  (str: string):string => {
    return `Hello ${str}`;
}
console.log(hello('Peter'));
